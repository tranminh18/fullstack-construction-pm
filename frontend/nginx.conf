server {
    listen 80;
    server_name _;
    root /usr/share/nginx/html;
    index index.html;

    # SPA: mọi route không khớp file tĩnh đều trả index.html
    # để React Router xử lý phía client.
    location / {
        try_files $uri $uri/ /index.html;
    }
}
